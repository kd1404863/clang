#include<stdio.h>
main()
{
	int tbl[][3] = { {10,20,30},{40,50,60},{70,80,90} };
	int* ptbl = &tbl[0][1], i;	//'&'を付ければ好きな配列から始めることが可能
	for (i = 0; i < 3; i++) {
		printf(" %d ", *ptbl);	//アドレスの中身を表示
		ptbl += 3;				//アドレスをint型(4バイト)分増加
	}
}